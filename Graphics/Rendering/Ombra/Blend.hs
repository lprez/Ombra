module Graphics.Rendering.Ombra.Blend (
        -- * Types
        Mode(..),
        Operator(..),
        Parameter(..),

        -- * Blending modes
        transparency,
        additive
) where

import Graphics.Rendering.Ombra.Blend.Internal
